<h2><%= @lesson.name %> - Lesson Number <%= @lesson.number %></h2>

<p><%= @lesson.description %></p>


<%= link_to "Back", section_lessons_path %>

<% content_for(:navbar) do %>
  <p class="pull-left"><%= link_to "Next lesson", section_lesson_path(@section, @lesson.next) %></p>
  <p class="pull-left"><%= link_to "Edit", edit_section_lesson_path %></p>
    <p class="pull-left"><%= link_to "Delete", section_lesson_path(@section, @lesson),
  :data => {:confirm => "You sure?",
    :method => 'delete'}%></p>


<% end %>
